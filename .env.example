VITE_SITE_NAME="GuedesLab"
VITE_CONTACT_EMAIL="herbertguedeslab@gmail.com"
VITE_CONTACT_ENDPOINT="/api/contact"

# Server-only (Vercel serverless function /api/contact.ts).
# NEVER prefix this with VITE_ — that would bundle it into the public client JS.
# Set the real value in the Vercel project's Environment Variables dashboard, not in a committed file.
RESEND_API_KEY=""
